apiVersion: apps/v1
kind: Deployment
metadata:
  name: multipaper-server
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: multipaper-server
  template:
    metadata:
      labels:
        app: multipaper-server
    spec:
      containers:
        - name: multipaper-server
          image: ghcr.io/matthew-beckett/multipaper-server
          env:
            - name: MULTIPAPER_MASTER_ADDRESS
              value: "127.0.0.1:255585"
          resources:
            limits:
              memory: 1.5G
              cpu: 500m
            requests:
              cpu: 250m
              memory: 1G
          ports:
            - name: multipaper
              containerPort: 25585
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 25585
            initialDelaySeconds: 30
            timeoutSeconds: 1
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            tcpSocket:
              port: 25585
            initialDelaySeconds: 30
            timeoutSeconds: 1
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 10